spring.application.name=finanzas

# Puerto y ruta base
server.port=8080
####server.servlet.context-path=/api

# Base de datos MySQL
spring.datasource.url=jdbc:mysql://${DATABASE_HOST:localhost}:${DATABASE_PORT:3309}/${DATABASE_NAME:finanzas_db}
spring.datasource.username=${MYSQL_USER}
spring.datasource.password=${MYSQL_PASSWORD}


# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# JWT (temporal)
jwt.secret=una_llave_secreta_de_32_o_mas_caracteres_2025!!

jwt.expiration=86400000
bcrypt.strength=${BCRYPT_STRENGTH}

logging.pattern.console = ${LOGPATTERN_CONSOLE:%green(%d{HH:mm:ss.SSS}) %blue(%-5level) %red([%thread]) %yellow(%logger{15}) - %msg%n}